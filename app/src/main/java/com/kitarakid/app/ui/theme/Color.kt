package com.kitarakid.app.ui.theme

import androidx.compose.ui.graphics.Color

// Charcoal stage-light palette: a dark backdrop with a single warm gold accent
// (the string-instrument namesake "kitara" and stage-lit warmth) plus a
// deep teal-green as a cooling counterweight so the gold doesn't carry everything.
val Charcoal = Color(0xFF121212)
val CharcoalSurface = Color(0xFF1B1B1B)
val CharcoalSurfaceRaised = Color(0xFF232323)

val Gold = Color(0xFFE8A33D)
val GoldDim = Color(0xFFB9812C)

val Teal = Color(0xFF1B4B43)
val TealBright = Color(0xFF2E7D6B)

val Cream = Color(0xFFF5F0E6)
val CreamDim = Color(0xFFA8A29A)

val Error = Color(0xFFCF6679)
